import 'package:flutter/material.dart';
import 'package:dart_twitter_api/twitter_api.dart';

class TwitterApi extends StatelessWidget {
  const TwitterApi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
